/** @type {import('next').NextConfig} */
const nextConfig = {
    /** @type {import('next').NextConfig} */
	experimental: {
		serverComponentsExternalPackages: ["oslo"]
	}
};


export default nextConfig;
